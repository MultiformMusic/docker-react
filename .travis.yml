language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t multiform/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true multiform/docker-react npm run test